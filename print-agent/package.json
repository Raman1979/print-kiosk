{
  "name": "print-kiosk-agent",
  "version": "1.0.0",
  "description": "Runs on the shop PC - polls the cloud server, downloads pending print jobs, sends them to the local printer, and deletes them for privacy.",
  "main": "agent.js",
  "type": "commonjs",
  "scripts": {
    "start": "node agent.js"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "dotenv": "^16.4.5"
  }
}
